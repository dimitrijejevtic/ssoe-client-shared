import { OnInit, EventEmitter } from '@angular/core';
import { MatAutocomplete, MatAutocompleteTrigger } from '@angular/material';
import { Subject } from 'rxjs';
import { SearchService } from '../../services/search.service';
import { TranslationService } from '../../services/translation.service';
import { FormControl, NgForm } from '@angular/forms';
import { ErrorMessagesService } from '../../services/error-messages.service';
export declare class TypeaheadInputComponent implements OnInit {
    private searchService;
    translateService: TranslationService;
    private formObject;
    private errorMessageService;
    readonly typeaheadSkipValue = 6;
    readonly minNumOfCharToSearch = 3;
    typeaheadValue: any;
    placeholder: string;
    typename: string;
    selectedValue: any;
    filterFormatFn: (value: any) => any;
    displayResultFn: (value: any) => string;
    propertyName: string;
    readonly: boolean;
    ngFormObject: NgForm;
    results: any[];
    skip: number;
    top: number;
    currentValue: string;
    hasErrors: boolean;
    inputChangeSubject: Subject<string>;
    ngControl: FormControl;
    isLoading: boolean;
    form: NgForm;
    private nextPage;
    valueChosen: boolean;
    typeaheadValueChange: EventEmitter<any>;
    typeaheadSelectedValueChange: EventEmitter<any>;
    autocomplete: MatAutocomplete;
    autocompleteTrigger: MatAutocompleteTrigger;
    constructor(searchService: SearchService, translateService: TranslationService, formObject: NgForm, errorMessageService: ErrorMessagesService);
    ngOnInit(): void;
    onScroll(): void;
    search(event: any): void;
    optionSelected(event: any): void;
    clearSelectedValue(): void;
    readonly checkHasValue: boolean;
    private resetFilter;
}

import { ClassificationValuePipe } from './classification-value.pipe';

describe('ClassificationValuePipe', () => {
  it('create an instance', () => {
    const pipe = new ClassificationValuePipe();
    expect(pipe).toBeTruthy();
  });
});

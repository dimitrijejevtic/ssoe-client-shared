import { classificationValueGet } from './classification-value-get.pipe';

describe('ClassificationValuePipe', () => {
  it('create an instance', () => {
    const pipe = new classificationValueGet();
    expect(pipe).toBeTruthy();
  });
});
